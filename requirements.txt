flask>=3.0.0
python-dotenv>=1.0.0
flask-cors>=4.0.0
tree-sitter>=0.22.0
tree-sitter-python>=0.22.0
gitpython>=3.1.40
anthropic>=0.25.0
click>=8.1.0
rich>=13.7.0
jinja2>=3.1.0
httpx>=0.27.0
watchdog>=4.0.0

Flask-Limiter>=3.5.0

# ── Feature 1: Async engine ────────────────────────────────────────────────────
# Optional — only needed when REDIS_URL is set. Install to enable Celery workers.
celery>=5.3.6
redis>=5.0.1
flower>=2.0.1
